// use std::collections::BinaryHeap;

/// @author: Leon
/// https://leetcode.com/problems/eliminate-maximum-number-of-monsters/
/// Time Complexity:    O()
/// Space Complexity:   O()
/// Ord is not implemented for f32
struct Solution;

#[allow(dead_code)]
impl Solution {
    // pub fn eliminate_maximum(dist: Vec<i32>, speed: Vec<i32>) -> i32 {
    //     let len_ms: usize = dist.len();
    //     let mut heap: BinaryHeap<f32> = {
    //         let mut heap: BinaryHeap<f32> = BinaryHeap::with_capacity(len_ms);
    //         for idx in 0..len_ms {
    //             heap.push(dist[idx] as f32 / speed[idx] as f32);
    //         }
    //         heap
    //     };
    //     let mut cnt: i32 = 0;
    //     while let Some(top) = heap.pop() {
    //         if top <= cnt {
    //             break;
    //         }
    //         cnt += 1;
    //     }
    //     return cnt;
    // }
}
